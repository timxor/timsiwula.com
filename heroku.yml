setup:
  addons:
  - plan: heroku-postgresql
build:
  docker:
    web: Dockerfile
    worker: worker/Dockerfile
release:
  image: web
  command:
    - python ./app/ch3/manage.py collectstatic --noinput
run:
  web: gunicorn django_project.wsgi
